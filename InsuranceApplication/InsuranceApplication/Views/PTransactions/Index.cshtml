@model InsuranceApplication.Models.PTransactionsByPolicyHolderViewModel

@{
    ViewData["Title"] = "Index";
}

<h1>Transactions</h1>
<form asp-controller="PTransactions" asp-action="Index" method="get">
    <p>

        <select asp-for="HolderName" asp-items="Model.Holders">
            <option value="">All</option>
        </select>

        Include Accepted: <input type="checkbox" asp-for="IncludeAccepted" />
        <input type="submit" value="Update" />
    </p>
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Transactions[0].DrugName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Transactions[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Transactions[0].Count)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Transactions[0].HolderName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Transactions[0].Accepted)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Transactions)
{
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DrugName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Count)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HolderName)
            </td>
            <td>
                @{ if (item.Accepted)
                    {
                        <p>Accepted</p>
                    }
                    else
                    {
                        <p>Not Accepted</p>
                    }
                }
            </td>
            <td>
                <a asp-action="Details" asp-route-id="@item.Id">Details</a>
            </td>
            <td>
                @if (!item.Accepted)
                {
                    <a asp-action="Accept" asp-route-id="@item.Id" class="btn btn-secondary btn-block">Accept</a>
                }
            </td>
        </tr>
}
    </tbody>
</table>
